export default class SessionDataRow {
  time: number;
  x: number;
  y: number;
  z: number;
  constructor(time: number, x: number, y: number, z: number) {
    this.time = time;
    this.x = x;
    this.y = y;
    this.z = z;
  }
}
